# Start from ubuntu image
FROM ubuntu:20.10

# Install OS dependencies
RUN apt-get update && apt-get install -y ca-certificates

# Copy binary
COPY rocketpool/rocketpool /go/bin/rocketpool

# Container entry point
ENTRYPOINT ["/go/bin/rocketpool"]
